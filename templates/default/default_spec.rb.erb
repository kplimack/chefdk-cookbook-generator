<%= license_description('#') %>
require_relative '../../spec_helper'

describe '<%= cookbook_name %>::default' do
  let(:chef_run) { ChefSpec::ServerRunner.converge(described_recipe) }

  it 'does something that you need to test' do
    expect(chef_run)
  end
end
